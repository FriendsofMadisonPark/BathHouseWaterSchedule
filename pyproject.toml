[project]
name = "madison-park-watering-schedule"
version = "0.1.0"
description = "Hello World starter for watering schedule project"
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
